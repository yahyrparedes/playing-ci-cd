pipeline {

  agent any
  stages {
    stage('build') {

      steps {
//         sh 'npm install'
            echo 'Start Build'
            echo 'Start Build'
      }
    }

    stage('test') {
      steps {
//         sh 'npm run test'
        echo 'Start Test'
      }
    }

  }
}